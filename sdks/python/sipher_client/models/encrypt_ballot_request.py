# coding: utf-8

"""
    Sipher — Privacy-as-a-Skill API

    REST API wrapping SIP Protocol's privacy SDK for Solana agents. Stealth addresses, Pedersen commitments, shielded transfers, and viewing key compliance.

    The version of the OpenAPI document: 0.1.0
    Contact: hello@sip-protocol.org
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class EncryptBallotRequest(BaseModel):
    """
    EncryptBallotRequest
    """ # noqa: E501
    proposal_id: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(description="Unique proposal identifier", alias="proposalId")
    vote: StrictStr = Field(description="Vote choice")
    voter_secret: Annotated[str, Field(strict=True)] = Field(description="Private entropy for nullifier derivation (never stored)", alias="voterSecret")
    stealth_address: Optional[StrictStr] = Field(default=None, description="Optional stealth address for voter anonymity (from /stealth/derive)", alias="stealthAddress")
    __properties: ClassVar[List[str]] = ["proposalId", "vote", "voterSecret", "stealthAddress"]

    @field_validator('vote')
    def vote_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['yes', 'no', 'abstain']):
            raise ValueError("must be one of enum values ('yes', 'no', 'abstain')")
        return value

    @field_validator('voter_secret')
    def voter_secret_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{64}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{64}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EncryptBallotRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EncryptBallotRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "proposalId": obj.get("proposalId"),
            "vote": obj.get("vote"),
            "voterSecret": obj.get("voterSecret"),
            "stealthAddress": obj.get("stealthAddress")
        })
        return _obj



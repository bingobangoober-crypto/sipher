
# BackendsHealth200ResponseDataMetrics


## Properties

Name | Type
------------ | -------------
`totalRequests` | number
`successfulRequests` | number
`failedRequests` | number
`averageLatencyMs` | number

## Example

```typescript
import type { BackendsHealth200ResponseDataMetrics } from '@sip-protocol/sipher-client'

// TODO: Update the object below with actual values
const example = {
  "totalRequests": null,
  "successfulRequests": null,
  "failedRequests": null,
  "averageLatencyMs": null,
} satisfies BackendsHealth200ResponseDataMetrics

console.log(example)

// Convert the instance to a JSON string
const exampleJSON: string = JSON.stringify(example)
console.log(exampleJSON)

// Parse the JSON string back to an object
const exampleParsed = JSON.parse(exampleJSON) as BackendsHealth200ResponseDataMetrics
console.log(exampleParsed)
```

[[Back to top]](#) [[Back to API list]](../README.md#api-endpoints) [[Back to Model list]](../README.md#models) [[Back to README]](../README.md)


